diff --git a/node_modules/pdf-parse/lib/pdf-parse.js b/node_modules/pdf-parse/lib/pdf-parse.js
index 4b6790a..50a8080 100644
--- a/node_modules/pdf-parse/lib/pdf-parse.js
+++ b/node_modules/pdf-parse/lib/pdf-parse.js
@@ -5,7 +5,6 @@
 
 //mock fs for browser builds
 const fs = require('fs');
-const path = require('path');
 
 
 module.exports = function(dataBuffer, options) {
@@ -19,10 +18,6 @@
 			max: 0,
 			version: 'default'
 		}, options || {});
-	} else if (typeof(PDFJS_PATH) === 'undefined') {
-		//in case of browser builds, we need to require pdfjs-dist without resorting to path module
-		//cannot use __dirname because it is not defined in browser
-		PDFJS_PATH = path.join(path.dirname(require.resolve('pdfjs-dist')), '..', `build/pdf.js`);
 	}
 
 	let ret = {
